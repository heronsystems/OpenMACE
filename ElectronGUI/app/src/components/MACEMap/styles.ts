const width = window.screen.width
const height = window.screen.height

export const styles = {
    parentStyle: {
        height: height + "px",
        width: width + "px"
    },
    mapStyle: {
        top: 64,
        left: 0,
        height: height + "px",
        width: width + "px"
    }
}
